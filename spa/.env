# URL of the BigTree PHP server. Vite dev server proxies /admin/api/* here so
# the SPA's requests look same-origin and the refresh cookie round-trips.
VITE_API_TARGET=https://bigtree.com/4.5/admin/api/v1/
